user:
  {% set dirs = salt['cp.list_master_dirs']('user', 'copy-to-vm') %}

  {% for dir in dirs if dir.startswith('copy-to-vm/') and dir.count('/') == 1 %}
  '{{ dir.split('/')[1] }}':
    - match: pcre
    - copy-to-vm
  {% endfor %}
